# ─── Local development ───
# Leave blank — Vite proxy (/ck-api → localhost:5000/api) is used automatically.
VITE_API_BASE_URL=

# Local receiver-onboarding app
VITE_ONBOARDING_BASE_URL=http://localhost:5174

# ─── Production (Vercel) ───
# Do NOT put live URLs here for daily work.
# Production uses `.env.production` → https://callkaro.delicod.com/api
# (applied automatically on `vite build` / Vercel).
